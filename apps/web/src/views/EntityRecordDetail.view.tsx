import React from "react";

// ── Types ────────────────────────────────────────────────────────────

export interface EntityRecordDetailViewProps {
  entityId: string;
  recordId: string;
}

// ── Container ────────────────────────────────────────────────────────

export const EntityRecordDetailView: React.FC<EntityRecordDetailViewProps> = (
  _props
) => {
  // TODO: Phase 6 — implement full view with entity + record data fetching,
  // metadata, and field value display.
  return null;
};
