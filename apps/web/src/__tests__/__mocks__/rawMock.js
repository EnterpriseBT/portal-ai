// Stub for Vite `?raw` imports under jest (#268). Tests that care about
// real srcdoc composition go through `buildSandboxSrcdoc` with fixture
// sources; the real `?raw` contents are a Vite build-time concern.
export default "";
