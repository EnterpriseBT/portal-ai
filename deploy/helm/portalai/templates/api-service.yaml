apiVersion: v1
kind: Service
metadata:
  name: {{ include "portalai.fullname" . }}-api
  labels:
    {{- include "portalai.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.api.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "portalai.selectorLabels" (dict "root" . "component" "api") | nindent 4 }}
